{
  "name": "@ocean/promise",
  "version": "0.0.13",
  "description": "@ocean/promise",
  "main": "dist/index.js",
  "typings": "dist/types/src/index.d.ts",
  "type": "module",
  "files": [
    "dist",
    "README.md"
  ],
  "keywords": [
    "ocean",
    "promise"
  ],
  "author": "OceanCommon",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "check": "tsc --noEmit",
    "build": "npm run clean && npm run check && vite build -c ../../vite.config.ts",
    "release": "pnpm run build& pnpm version patch & pnpm publish --no-git-checks"
  },
  "dependencies": {
    "@ocean/common": "^0.0.57",
    "@ocean/promise": "file:",
    "@ocean/reaction": "^0.0.12"
  }
}
