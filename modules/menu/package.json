{
  "$schema": "https://json.schemastore.org/package",
  "name": "@msom/menu",
  "version": "0.0.1",
  "description": "@msom/menu",
  "main": "dist/index.js",
  "typings": "dist/types/index.d.ts",
  "type": "module",
  "files": [
    "dist",
    "README.md"
  ],
  "keywords": [
    "ocean",
    "menu"
  ],
  "author": "OceanCommon",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "check": "tsc --noEmit",
    "version": "node ../../scripts/version.js",
    "build": "pnpm run clean && pnpm run check && xbuild build -c ../../vite.config.ts",
    "compile": "xbuild build -C -c ../../vite.config.ts",
    "release": "pnpm run build && pnpm run version --patch && pnpm publish --no-git-checks"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@msom/menu": "file:"
  }
}
